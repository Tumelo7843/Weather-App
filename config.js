// Optional: set this file on GitHub Pages to point the frontend at your Render backend.
// Example (replace with your Render service URL):
// window.BACKEND_URL = 'https://your-service.onrender.com';

// Leave empty for same-origin backend (when server serves frontend):
window.BACKEND_URL = '';
